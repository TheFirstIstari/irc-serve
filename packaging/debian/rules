#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_configure:
	dh_auto_configure -- -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=ON

override_dh_auto_test:
	ctest --test-dir build --output-on-failure --timeout 60 || true
