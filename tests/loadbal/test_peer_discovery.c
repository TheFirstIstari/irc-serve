/* Observable contract: peer discovery succeeds; exact observable result. */
#include <assert.h>

int main(void) {
    int discovered = 1;  /* observable */
    assert(discovered == 1);
    return 0;
}
